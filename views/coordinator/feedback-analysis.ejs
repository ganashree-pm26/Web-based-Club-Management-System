<!DOCTYPE html>
<html>
<head>
    <title>Feedback Analysis - <%= user.role === 'admin' ? 'Admin' : 'Coordinator' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body {
            min-height: 100vh;
            background: radial-gradient(circle at top left, #4f46e5 0, #0f172a 45%, #020617 100%);
            color: #e5e7eb;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            padding: 24px 0;
        }
        .glass-navbar {
            background: rgba(15, 23, 42, 0.95) !important;
            border-bottom: 1px solid rgba(148, 163, 184, 0.3);
            backdrop-filter: blur(10px);
        }
        .card {
            background: rgba(15, 23, 42, 0.9) !important;
            border: 1px solid rgba(148, 163, 184, 0.3) !important;
            border-radius: 18px !important;
            color: #e5e7eb !important;
            box-shadow: 0 18px 45px rgba(15, 23, 42, 0.65) !important;
        }
        .alert {
            background: rgba(15, 23, 42, 0.9) !important;
            border: 1px solid rgba(148, 163, 184, 0.3) !important;
            color: #e5e7eb !important;
            border-radius: 12px !important;
        }
        .alert-info {
            background: rgba(59, 130, 246, 0.2) !important;
            border-color: rgba(59, 130, 246, 0.4) !important;
            color: #93c5fd !important;
        }
        h1, h2, h3, h4, h5, h6 {
            color: #e5e7eb !important;
        }
        .text-muted {
            color: #94a3b8 !important;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark glass-navbar mb-4">
        <div class="container-fluid">
            <span class="navbar-brand">Feedback Analysis Dashboard</span>
            <div>
                <% if (user.role === 'admin') { %>
                    <a href="/admin/dashboard" class="btn btn-outline-light btn-sm me-2">← Admin Dashboard</a>
                <% } else { %>
                    <a href="/coordinator/dashboard" class="btn btn-outline-light btn-sm me-2">← Dashboard</a>
                <% } %>
                <a href="/logout" class="btn btn-outline-light btn-sm">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h2 class="text-white">Feedback Analysis</h2>
            <p class="text-muted">Analyze participant feedback to identify recurring issues and improvement opportunities</p>
        </div>

        <% if (events.length === 0) { %>
            <div class="alert alert-info">
                <h5>No events with feedback available</h5>
                <% if (user.role === 'admin') { %>
                    <p>There are no events in the system that have received participant feedback yet.</p>
                <% } else { %>
                    <p>There are no events assigned to you that have received participant feedback yet.</p>
                <% } %>
            </div>
        <% } else { %>
            <div class="row">
                <% events.forEach(event => { %>
                    <div class="col-md-6 col-lg-4 mb-4">
                        <div class="card h-100">
                            <div class="card-body">
                                <h5 class="card-title"><%= event.EventName %></h5>
                                <p class="card-text">
                                    <small class="text-muted">Date: <%= new Date(event.EventDate).toLocaleDateString() %></small><br>
                                    <small class="text-muted">Feedback Received: <%= event.totalFeedback %></small>
                                </p>
                                <a href="/feedback-analysis/event/<%= event.EventID %>" class="btn btn-primary">Analyze Feedback</a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>