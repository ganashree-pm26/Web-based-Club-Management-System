<!DOCTYPE html>
<html>
<head>
    <title>My Tasks - Member Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            min-height: 100vh;
            background: radial-gradient(circle at top left, #4f46e5 0, #0f172a 45%, #020617 100%);
            color: #e5e7eb;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            padding: 24px 0;
        }
        .glass-card {
            background: rgba(15, 23, 42, 0.9);
            border-radius: 18px;
            border: 1px solid rgba(148, 163, 184, 0.3);
            box-shadow: 0 18px 45px rgba(15, 23, 42, 0.65);
            color: #e5e7eb;
        }
        .glass-navbar {
            background: rgba(15, 23, 42, 0.95) !important;
            border-bottom: 1px solid rgba(148, 163, 184, 0.3);
            backdrop-filter: blur(10px);
        }
        .task-card {
            background: rgba(15, 23, 42, 0.9);
            border-left: 4px solid rgba(148, 163, 184, 0.3);
            border-radius: 18px;
            transition: all 0.3s;
            margin-bottom: 15px;
            border: 1px solid rgba(148, 163, 184, 0.3);
            box-shadow: 0 18px 45px rgba(15, 23, 42, 0.65);
            color: #e5e7eb;
        }
        .task-card:hover {
            box-shadow: 0 24px 60px rgba(15, 23, 42, 0.8);
            transform: translateY(-2px);
            border-color: rgba(148, 163, 184, 0.5);
        }
        .task-card.pending {
            border-left-color: #6c757d;
        }
        .task-card.in-progress {
            border-left-color: #ffc107;
        }
        .task-card.completed {
            border-left-color: #198754;
        }
        .stat-badge {
            font-size: 0.85rem;
            padding: 0.35rem 0.65rem;
        }
        .deadline-badge {
            font-size: 0.75rem;
        }
        .card {
            background: rgba(15, 23, 42, 0.9) !important;
            border: 1px solid rgba(148, 163, 184, 0.3) !important;
            border-radius: 18px !important;
            color: #e5e7eb !important;
        }
        .text-muted {
            color: #94a3b8 !important;
        }
        h1, h2, h3, h4, h5, h6 {
            color: #e5e7eb !important;
        }
        .alert {
            background: rgba(15, 23, 42, 0.9) !important;
            border: 1px solid rgba(148, 163, 184, 0.3) !important;
            color: #e5e7eb !important;
            border-radius: 12px !important;
        }
        .alert-info {
            background: rgba(59, 130, 246, 0.2) !important;
            border-color: rgba(59, 130, 246, 0.4) !important;
            color: #93c5fd !important;
        }
        .form-control, .form-select {
            background: rgba(30, 41, 59, 0.8) !important;
            border: 1px solid rgba(148, 163, 184, 0.3) !important;
            color: #e5e7eb !important;
        }
        .form-control:focus, .form-select:focus {
            background: rgba(30, 41, 59, 0.9) !important;
            border-color: #4f46e5 !important;
            color: #e5e7eb !important;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark glass-navbar mb-4">
        <div class="container-fluid">
            <span class="navbar-brand">
                <strong>Member Dashboard</strong> - <%= member.MemberName || user.name || user.username %>
            </span>
            <div class="d-flex align-items-center gap-3">
                <span class="text-light small">
                    <%= member.Email || '' %> | <%= member.Phone || '' %>
                </span>
                <a href="/logout" class="btn btn-outline-light btn-sm">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="row mb-4">
            <div class="col-md-12">
                <h2 class="mb-3 text-white">My Assigned Tasks</h2>
                
                <% if(tasks.length === 0) { %>
                    <div class="alert alert-info">
                        <h5>No tasks assigned yet!</h5>
                        <p class="mb-0">You don't have any tasks assigned to you at the moment. Check back later or contact your coordinator.</p>
                    </div>
                <% } else { %>
                    <div class="row mb-3">
                        <div class="col-md-12">
                            <div class="card">
                                <div class="card-body">
                                    <div class="d-flex justify-content-between align-items-center mb-3">
                                        <h5 class="mb-0">Task Summary</h5>
                                        <div>
                                            <span class="badge bg-secondary stat-badge me-2">
                                                Pending: <%= tasks.filter(t => t.Status === 'Pending').length %>
                                            </span>
                                            <span class="badge bg-warning stat-badge me-2">
                                                In Progress: <%= tasks.filter(t => t.Status === 'In Progress').length %>
                                            </span>
                                            <span class="badge bg-success stat-badge">
                                                Completed: <%= tasks.filter(t => t.Status === 'Completed').length %>
                                            </span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <% tasks.forEach(task => { 
                            const deadlineDate = task.Deadline ? new Date(task.Deadline) : null;
                            const isOverdue = deadlineDate && deadlineDate < new Date() && task.Status !== 'Completed';
                            const statusClass = task.Status.toLowerCase().replace(' ', '-');
                        %>
                            <div class="col-md-6 mb-3">
                                <div class="card task-card <%= statusClass %>">
                                    <div class="card-body">
                                        <div class="d-flex justify-content-between align-items-start mb-2">
                                            <h5 class="card-title mb-0"><%= task.TaskName %></h5>
                                            <span class="badge <%= task.Status === 'Completed' ? 'bg-success' : task.Status === 'In Progress' ? 'bg-warning' : 'bg-secondary' %>">
                                                <%= task.Status %>
                                            </span>
                                        </div>
                                        
                                        <% if(task.Description) { %>
                                            <p class="card-text text-muted small mb-2"><%= task.Description %></p>
                                        <% } %>
                                        
                                        <div class="mb-3">
                                            <small class="text-muted">
                                                <strong>Event:</strong> <%= task.EventName %><br>
                                                <% if(task.EventDate) { %>
                                                    <strong>Event Date:</strong> <%= new Date(task.EventDate).toLocaleDateString() %><br>
                                                <% } %>
                                                <% if(task.Venue) { %>
                                                    <strong>Venue:</strong> <%= task.Venue %><br>
                                                <% } %>
                                                <% if(deadlineDate) { %>
                                                    <strong>Deadline:</strong> 
                                                    <span class="badge <%= isOverdue ? 'bg-danger' : 'bg-info' %> deadline-badge">
                                                        <%= deadlineDate.toLocaleDateString() %>
                                                        <% if(isOverdue) { %> (Overdue) <% } %>
                                                    </span>
                                                <% } else { %>
                                                    <strong>Deadline:</strong> <span class="text-muted">Not set</span>
                                                <% } %>
                                            </small>
                                        </div>

                                        <form action="/members/tasks/<%= task.TaskID %>/status" method="POST" class="mt-3">
                                            <div class="input-group">
                                                <select name="status" class="form-select" onchange="this.form.submit()">
                                                    <option value="Pending" <%= task.Status === 'Pending' ? 'selected' : '' %>>Pending</option>
                                                    <option value="In Progress" <%= task.Status === 'In Progress' ? 'selected' : '' %>>In Progress</option>
                                                    <option value="Completed" <%= task.Status === 'Completed' ? 'selected' : '' %>>Completed</option>
                                                </select>
                                                <button type="submit" class="btn btn-primary">Update Status</button>
                                            </div>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } %>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
