<!DOCTYPE html>
<html>
<head>
    <title>Sponsor Created - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark bg-dark mb-4">
        <div class="container-fluid">
            <span class="navbar-brand">Sponsor Created Successfully</span>
            <div>
                <a href="/admin/dashboard" class="btn btn-outline-light btn-sm me-2">← Dashboard</a>
                <a href="/admin/sponsors" class="btn btn-outline-light btn-sm me-2">All Sponsors</a>
                <a href="/logout" class="btn btn-outline-light btn-sm">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header bg-success text-white">
                        <h3 class="mb-0">Sponsor Account Created Successfully!</h3>
                    </div>
                    <div class="card-body">
                        <div class="alert alert-success">
                            <h5>Account Details</h5>
                            <p><strong>Sponsor Name:</strong> <%= sponsor.name %></p>
                            <p><strong>Contribution:</strong> ₹<%= (sponsor.contribution || 0).toLocaleString() %></p>
                            <p><strong>Event:</strong> <%= sponsor.eventId ? 'Assigned' : 'No Event Assigned' %></p>
                            <p><strong>Username:</strong> <code><%= username %></code></p>
                            <p><strong>Password:</strong> <code><%= password %></code></p>
                        </div>

                        <div class="alert alert-info">
                            <h5>Important Information</h5>
                            <ul>
                                <li>Please share these credentials securely with the sponsor</li>
                                <li>The sponsor can now login to the system using the above credentials</li>
                                <li>Username: <code><%= username %></code></li>
                                <li>Password: <code><%= password %></code></li>
                                <li>Login URL: <a href="/login">/login</a></li>
                            </ul>
                        </div>

                        <div class="mb-3">
                            <h5>Next Steps for Sponsor:</h5>
                            <ol>
                                <li>Go to the login page</li>
                                <li>Select "Sponsor" role</li>
                                <li>Enter the username and password provided above</li>
                                <li>Login and start viewing assigned event details and budget information</li>
                            </ol>
                        </div>

                        <div class="d-grid gap-2">
                            <a href="/admin/sponsors" class="btn btn-primary">Manage Sponsors</a>
                            <a href="/admin/dashboard" class="btn btn-secondary">Back to Dashboard</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>