<!DOCTYPE html>
<html>
<head>
    <title>Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            min-height: 100vh;
            background: radial-gradient(circle at top left, #4f46e5 0, #0f172a 45%, #020617 100%);
            color: #e5e7eb;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            padding: 24px 0;
        }
        .glass-card {
            background: rgba(15, 23, 42, 0.9);
            border-radius: 18px;
            border: 1px solid rgba(148, 163, 184, 0.3);
            box-shadow: 0 18px 45px rgba(15, 23, 42, 0.65);
            color: #e5e7eb;
        }
        .glass-navbar {
            background: rgba(15, 23, 42, 0.95) !important;
            border-bottom: 1px solid rgba(148, 163, 184, 0.3);
            backdrop-filter: blur(10px);
        }
        .stat-card {
            background: linear-gradient(135deg, rgba(79, 70, 229, 0.3) 0%, rgba(139, 92, 246, 0.3) 100%);
            color: white;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 20px;
            border: 1px solid rgba(148, 163, 184, 0.3);
        }
        .event-card {
            background: rgba(15, 23, 42, 0.9);
            border: 1px solid rgba(148, 163, 184, 0.3);
            border-radius: 18px;
            padding: 15px;
            margin-bottom: 15px;
            transition: all 0.3s;
            color: #e5e7eb;
            box-shadow: 0 18px 45px rgba(15, 23, 42, 0.65);
        }
        .event-card:hover {
            box-shadow: 0 24px 60px rgba(15, 23, 42, 0.8);
            transform: translateY(-2px);
            border-color: rgba(148, 163, 184, 0.5);
        }
        .menu-card {
            background: rgba(15, 23, 42, 0.9);
            border: 1px solid rgba(148, 163, 184, 0.3);
            border-radius: 18px;
            padding: 20px;
            text-align: center;
            transition: all 0.3s;
            height: 100%;
            color: #e5e7eb;
            box-shadow: 0 18px 45px rgba(15, 23, 42, 0.65);
        }
        .menu-card:hover {
            box-shadow: 0 24px 60px rgba(15, 23, 42, 0.8);
            transform: translateY(-2px);
            border-color: rgba(148, 163, 184, 0.5);
        }
        .text-muted {
            color: #94a3b8 !important;
        }
        h1, h2, h3, h4, h5, h6 {
            color: #e5e7eb !important;
        }
        .alert {
            background: rgba(15, 23, 42, 0.9) !important;
            border: 1px solid rgba(148, 163, 184, 0.3) !important;
            color: #e5e7eb !important;
            border-radius: 12px !important;
        }
        .alert-info {
            background: rgba(59, 130, 246, 0.2) !important;
            border-color: rgba(59, 130, 246, 0.4) !important;
            color: #93c5fd !important;
        }
        .progress {
            background: rgba(30, 41, 59, 0.8) !important;
            border-radius: 8px !important;
        }
        .event-actions {
            padding-top: 0;
        }
        @media (min-width: 768px) {
            .event-actions {
                padding-top: calc(1.5rem + 1.5rem + 0.5rem);
            }
        }
        .btn-allocate {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            border: none;
            color: white !important;
            font-weight: 500;
        }
        .btn-allocate:hover {
            background: linear-gradient(135deg, #3b9dfd 0%, #00d9ed 100%);
            transform: translateY(-1px);
            box-shadow: 0 8px 20px rgba(79, 172, 254, 0.4);
        }
        .btn-edit {
            background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
            border: none;
            color: #1f2937 !important;
            font-weight: 500;
        }
        .btn-edit:hover {
            background: linear-gradient(135deg, #f35784 0%, #fdd835 100%);
            transform: translateY(-1px);
            box-shadow: 0 8px 20px rgba(250, 112, 154, 0.4);
        }
        .btn-delete {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            border: none;
            color: white !important;
            font-weight: 500;
        }
        .btn-delete:hover {
            background: linear-gradient(135deg, #e17fef 0%, #e84660 100%);
            transform: translateY(-1px);
            box-shadow: 0 8px 20px rgba(240, 147, 251, 0.4);
        }
        .btn-download {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            color: white !important;
            font-weight: 500;
        }
        .btn-download:hover {
            background: linear-gradient(135deg, #5568d3 0%, #6a3f8f 100%);
            transform: translateY(-1px);
            box-shadow: 0 8px 20px rgba(102, 126, 234, 0.4);
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark glass-navbar mb-4">
        <div class="container-fluid">
            <span class="navbar-brand">Admin Dashboard - <%= user.name || user.username %></span>
            <a href="/logout" class="btn btn-outline-light btn-sm">Logout</a>
        </div>
    </nav>

    <div class="container">
        <h2 class="mb-4 text-white">Admin Control Panel</h2>

        <!-- Quick Actions Menu -->
        <div class="row mb-4">
            <div class="col-md-3 mb-3">
                <div class="menu-card">
                    <h5>📅 Events</h5>
                    <p class="text-muted small">Manage all events</p>
                    <a href="/events" class="btn btn-primary btn-sm">Manage Events</a>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="menu-card">
                    <h5>👥 Coordinators</h5>
                    <p class="text-muted small">Create & manage coordinators</p>
                    <a href="/admin/coordinators" class="btn btn-success btn-sm">Manage Coordinators</a>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="menu-card">
                    <h5>👤 Members</h5>
                    <p class="text-muted small">Create & manage members</p>
                    <a href="/admin/members" class="btn btn-info btn-sm">Manage Members</a>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="menu-card">
                    <h5>💰 Expenditures</h5>
                    <p class="text-muted small">Review & approve expenses</p>
                    <a href="/admin/expenditures" class="btn btn-warning btn-sm">View Expenditures</a>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="menu-card">
                    <h5>📊 Reports</h5>
                    <p class="text-muted small">Budget utilization reports</p>
                    <a href="/admin/reports/budget-utilization" class="btn btn-info btn-sm">View Reports</a>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="menu-card">
                    <h5>🏛️ Clubs</h5>
                    <p class="text-muted small">Create & manage clubs</p>
                    <a href="/clubs" class="btn btn-secondary btn-sm">Manage Clubs</a>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="menu-card">
                    <h5>💰 Sponsors</h5>
                    <p class="text-muted small">Create & manage sponsors</p>
                    <a href="/admin/sponsors" class="btn btn-warning btn-sm">Manage Sponsors</a>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="menu-card">
                    <h5>📈 Income</h5>
                    <p class="text-muted small">Manage event income</p>
                    <a href="/income" class="btn btn-success btn-sm">Manage Income</a>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="menu-card">
                    <h5>📈 Analytics</h5>
                    <p class="text-muted small">Data analytics for decision support</p>
                    <a href="/admin/analytics" class="btn btn-primary btn-sm">View Analytics</a>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="menu-card">
                    <h5>💬 Feedback</h5>
                    <p class="text-muted small">Analyze participant feedback</p>
                    <a href="/feedback-analysis/dashboard" class="btn btn-secondary btn-sm">Analyze Feedback</a>
                </div>
            </div>
        </div>

        <!-- Events Overview -->
        <h3 class="mb-3 text-white">Events Overview</h3>
        <% if(events.length === 0) { %>
            <div class="alert alert-info">No events found. <a href="/events/create">Create your first event</a></div>
        <% } else { %>
            <% events.forEach(event => { 
                const utilization = event.totalBudget > 0 ? (event.totalSpent / event.totalBudget * 100) : 0;
                const progressWidth = Math.min(utilization, 100);
            %>
                <div class="event-card">
                    <div class="row" style="align-items: stretch;">
                        <div class="col-md-8">
                            <h4><%= event.EventName %></h4>
                            <p class="text-muted mb-2">
                                <strong>Date:</strong> <%= new Date(event.EventDate).toLocaleDateString() %> | 
                                <strong>Venue:</strong> <%= event.Venue %>
                            </p>
                            <div class="row mb-3">
                                <div class="col-md-3">
                                    <div class="stat-card text-center" style="background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);">
                                        <h5><%= event.totalParticipants || 0 %></h5>
                                        <small>Participants</small>
                                    </div>
                                </div>
                                <div class="col-md-3">
                                    <div class="stat-card text-center" style="background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);">
                                        <h5>₹<%= (event.totalBudget || 0).toLocaleString() %></h5>
                                        <small>Budget</small>
                                    </div>
                                </div>
                                <div class="col-md-3">
                                    <div class="stat-card text-center" style="background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);">
                                        <h5>₹<%= (event.totalSpent || 0).toLocaleString() %></h5>
                                        <small>Spent</small>
                                    </div>
                                </div>
                                <div class="col-md-3">
                                    <div class="stat-card text-center" style="background: linear-gradient(135deg, #fa709a 0%, #fee140 100%);">
                                        <h5><%= event.totalFeedback || 0 %></h5>
                                        <small>Feedback</small>
                                    </div>
                                </div>
                            </div>
                            <div class="mb-2">
                                <small class="text-muted">Budget Utilization: </small>
                                <div class="progress" style="height: 20px;">
                                    <div class="progress-bar bg-success" 
                                         role="progressbar" 
                                         data-width="<%= progressWidth %>"
                                         aria-valuenow="<%= progressWidth %>" 
                                         aria-valuemin="0" 
                                         aria-valuemax="100">
                                        <%= utilization.toFixed(1) %>%
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-4 event-actions d-flex flex-column justify-content-start">
                            <a href="/admin/events/<%= event.EventID %>/download-pdf" class="btn btn-download btn-sm w-100 mb-2" download>📥 Download PDF</a>
                            <a href="/admin/events/<%= event.EventID %>/budget" class="btn btn-allocate btn-sm w-100 mb-2">Allocate Budget</a>
                            <a href="/events/edit/<%= event.EventID %>" class="btn btn-edit btn-sm w-100 mb-2">Edit Event</a>
                            <a href="/events/delete/<%= event.EventID %>" class="btn btn-delete btn-sm w-100" onclick="return confirm('Delete this event?')">Delete Event</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Set progress bar widths from data attributes
        document.querySelectorAll('.progress-bar[data-width]').forEach(bar => {
            const width = bar.getAttribute('data-width');
            bar.style.width = width + '%';
        });
    </script>
</body>
</html>
