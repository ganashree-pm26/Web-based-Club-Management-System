<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Club Management System – Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      min-height: 100vh;
      background: radial-gradient(circle at top left, #4f46e5 0, #0f172a 45%, #020617 100%);
      color: #e5e7eb;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }
    .glass-card {
      background: rgba(15, 23, 42, 0.9);
      border-radius: 18px;
      border: 1px solid rgba(148, 163, 184, 0.3);
      box-shadow: 0 18px 45px rgba(15, 23, 42, 0.65);
    }
    .login-label {
      font-size: 13px;
      font-weight: 600;
      color: #e5e7eb;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row g-4 align-items-center justify-content-center">
      <!-- Left: App info -->
      <div class="col-lg-6">
        
        <h1 class="hero-title display-5 mb-3">Club Event Management.</h1>
        <p class="mb-4 text-secondary">
          Manage college events, teams, and budgets in one place.
          This dashboard helps you track events, participants, tasks, sponsors and feedback for every club activity.
          
        </p>
        
      </div>

      <!-- Right: Login card -->
      <div class="col-lg-5 col-xl-4">
        <div class="glass-card p-4 p-md-5">
          <h4 class="mb-1 text-white text-center">Sign in</h4>
          <p class="text-secondary mb-3 text-center">Enter your credentials to access the system</p>

          <% if(error){ %>
            <div class="alert alert-danger py-2"><%= error %></div>
          <% } %>

          <form method="POST" action="/login" class="mt-3">
            <div class="mb-3">
              <label class="login-label">Role</label>
              <select name="role" class="form-select form-select-sm" required>
                <option value="">Select role</option>
                <option value="admin">Admin</option>
                <option value="coordinator">Coordinator</option>
                <option value="member">Member</option>
                <option value="sponsor">Sponsor</option>
                <option value="participant">Participant</option>
              </select>
            </div>

            <div class="mb-3">
              <label class="login-label">Username</label>
              <input name="username" class="form-control form-control-sm" placeholder="e.g. admin, coord_ganashree" required />
            </div>

            <div class="mb-3">
              <label class="login-label">Password</label>
              <input name="password" type="password" class="form-control form-control-sm" placeholder="Enter admin password" required />
            </div>

            

            <button class="btn btn-primary w-100 mb-2">Sign in</button>

         
            
            <hr class="my-3">
            
            <p class="small text-center mb-0">
              <strong>New Participant?</strong> 
              <a href="/participants/signup" class="text-primary">Sign up here</a>
            </p>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>