<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Club Management System – Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      min-height: 100vh;
      background: radial-gradient(circle at top left, #4f46e5 0, #0f172a 45%, #020617 100%);
      color: #e5e7eb;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }
    .hero-title {
      font-weight: 700;
      letter-spacing: .03em;
    }
    .glass-card {
      background: rgba(15, 23, 42, 0.9);
      border-radius: 18px;
      border: 1px solid rgba(148, 163, 184, 0.3);
      box-shadow: 0 18px 45px rgba(15, 23, 42, 0.65);
    }
    .pill {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 4px 12px;
      border-radius: 999px;
      background: rgba(15,118,110,0.15);
      color: #a5f3fc;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: .08em;
    }
    .role-chip {
      font-size: 12px;
      padding: 2px 9px;
      border-radius: 999px;
      border: 1px solid rgba(148, 163, 184, 0.4);
      color: #cbd5f5;
    }
    .role-chip span {
      opacity: 0.7;
    }
    .login-label {
      font-size: 13px;
      font-weight: 600;
      color: #e5e7eb;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row g-4 align-items-center justify-content-center">
      <!-- Left: App info -->
      <div class="col-lg-6">
        <div class="mb-3 pill">
          
        </div>
        <h1 class="hero-title display-5 mb-3">Club Event Management</h1>
        <p class="mb-4 text-secondary">
          Manage college events, teams, and budgets in one place.<br>
          This dashboard helps you track events, participants, tasks, sponsors and feedback for every club activity.
        </p>
      </div>

      <!-- Right: Login card -->
      <div class="col-lg-5 col-xl-4">
        <div class="glass-card p-4 p-md-5">
          <h4 class="mb-1 text-white">Sign in</h4>
          <p class="text-secondary mb-3">Choose your role and use the corresponding credentials.</p>

          <% if(error){ %>
            <div class="alert alert-danger py-2"><%= error %></div>
          <% } %>

          <form method="POST" action="/login" class="mt-3">
            <div class="mb-2">
              <label class="login-label">Role</label>
              <select name="role" class="form-select form-select-sm" required>
                <option value="">Select role</option>
                <option value="admin">Admin</option>
                <option value="coordinator">Coordinator</option>
                <option value="member">Member</option>
                <option value="sponsor">Sponsor</option>
                <option value="participant">Participant</option>
              </select>
            </div>

            <div class="mb-3">
              <label class="login-label">Username</label>
              <input name="username" class="form-control form-control-sm" placeholder="Enter your username" required />
            </div>

            <div class="mb-2">
              <label class="login-label">Password</label>
              <input name="password" type="password" class="form-control form-control-sm" placeholder="Enter your password" required />
            </div>

            <button class="btn btn-primary w-100 mb-2">Sign in</button>
            
            <hr class="my-3">
            
            <p class="small text-center mb-0">
              <strong>New Participant?</strong> 
              <a href="/participants/signup" class="text-primary">Sign up here</a> to register for events and get login credentials.
            </p>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>