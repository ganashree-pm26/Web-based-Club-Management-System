<!DOCTYPE html>
<html>
<head>
    <title>Club Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 80px 0;
            margin-bottom: 50px;
        }
        .feature-card {
            border: 1px solid #dee2e6;
            border-radius: 10px;
            padding: 30px;
            text-align: center;
            transition: all 0.3s;
            height: 100%;
        }
        .feature-card:hover {
            box-shadow: 0 6px 12px rgba(0,0,0,0.1);
            transform: translateY(-5px);
        }
        .cta-button {
            padding: 15px 40px;
            font-size: 1.2rem;
            border-radius: 50px;
        }
    </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-dark bg-dark">
        <div class="container-fluid">
            <span class="navbar-brand">Club Management System</span>
            <% if(user) { %>
                <a href="/logout" class="btn btn-outline-light btn-sm">Logout</a>
            <% } else { %>
                <a href="/login" class="btn btn-outline-light btn-sm">Login</a>
            <% } %>
        </div>
    </nav>

    <div class="hero-section">
        <div class="container text-center">
            <h1 class="display-3 mb-4">Welcome to Club Management System</h1>
            <p class="lead mb-5">Manage events, participants, budgets, and more in one place</p>
            
            <% if(!user) { %>
                <div class="d-flex gap-3 justify-content-center flex-wrap">
                    <a href="/participants/signup" class="btn btn-light cta-button">
                        🎯 Register as Participant
                    </a>
                    <a href="/login" class="btn btn-outline-light cta-button">
                        🔐 Login
                    </a>
                </div>
            <% } else { %>
                <a href="/<%= user.role === 'admin' ? 'admin' : user.role === 'coordinator' ? 'coordinator' : user.role === 'member' ? 'members' : user.role === 'sponsor' ? 'sponsors' : 'participants' %>/dashboard" class="btn btn-light cta-button">
                    Go to Dashboard
                </a>
            <% } %>
        </div>
    </div>

    <div class="container">
        <h2 class="text-center mb-5">How It Works</h2>
        
        <div class="row mb-5">
            <div class="col-md-4 mb-4">
                <div class="feature-card">
                    <div class="mb-3" style="font-size: 3rem;">🎯</div>
                    <h4>For Participants</h4>
                    <p class="text-muted">New to our system? Sign up to register for events, make payments, and get your login credentials instantly!</p>
                    <a href="/participants/signup" class="btn btn-primary">Sign Up Now</a>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="feature-card">
                    <div class="mb-3" style="font-size: 3rem;">👥</div>
                    <h4>For Coordinators</h4>
                    <p class="text-muted">Manage your events, participants, tasks, and budgets all from your dashboard.</p>
                    <a href="/login" class="btn btn-success">Coordinator Login</a>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="feature-card">
                    <div class="mb-3" style="font-size: 3rem;">👨‍💼</div>
                    <h4>For Admins</h4>
                    <p class="text-muted">Full system control - manage events, coordinators, budgets, and view comprehensive reports.</p>
                    <a href="/login" class="btn btn-warning">Admin Login</a>
                </div>
            </div>
        </div>

        <div class="card mb-5">
            <div class="card-header bg-primary text-white">
                <h4 class="mb-0">🚀 Quick Start Guide for Participants</h4>
            </div>
            <div class="card-body">
                <ol class="mb-0">
                    <li class="mb-3">
                        <strong>Step 1: Sign Up</strong><br>
                        Click on <a href="/participants/signup">"Register as Participant"</a> or visit <code>/participants/signup</code>
                    </li>
                    <li class="mb-3">
                        <strong>Step 2: Browse Events</strong><br>
                        View all available upcoming events with details like date, venue, and registered participants
                    </li>
                    <li class="mb-3">
                        <strong>Step 3: Select Event</strong><br>
                        Click on an event card to select it for registration
                    </li>
                    <li class="mb-3">
                        <strong>Step 4: Fill Registration Form</strong><br>
                        Enter your details (name, email, phone), team info (optional), payment amount, and create a password
                    </li>
                    <li class="mb-3">
                        <strong>Step 5: Get Credentials</strong><br>
                        After successful registration, you'll receive your username and password
                    </li>
                    <li>
                        <strong>Step 6: Login</strong><br>
                        Use your credentials to login at <a href="/login">/login</a> and access your participant dashboard
                    </li>
                </ol>
            </div>
        </div>

        <div class="text-center mb-5">
            <h3>Ready to Get Started?</h3>
            <a href="/participants/signup" class="btn btn-primary btn-lg">Register as Participant</a>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-4 mt-5">
        <div class="container">
            <p class="mb-0">&copy; 2025 Club Management System - DBMS Project</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

